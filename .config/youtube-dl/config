#  youtube-dl options
# -------------------- ---- -- -

# format selection (ffmpeg required)
-f "bestvideo[ext!=webm][height<=720]+bestaudio[ext!=webm]/bestvideo+bestaudio/best"

# output filename
-o "%(title)s #%(id)s.%(ext)s"

# download the video description as well
--write-description
